To compile the programs:

$> cc `pkg-config --cflags gtk+-3.0 libgdata` <file_name>.c -o hello `pkg-config --libs gtk+-3.0 libgdata goa-1.0`
$> ./hello

Make sure you have corresponding devel packages installed.
